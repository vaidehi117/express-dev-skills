<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>Express All Skills</title>
</head>
<body>
    <h1>Skills</h1>
    <!-- this is ow the client makes an http get request to the server, for a new todo form  -->
    <a href="/skills/new">New skill</a>
    <ul>
      <% skills.forEach(function(t) { %>
        <li>
          <a href="/skills/<%= t.id%>"><%= t.skill %></a>
            - 
          <%= t.done ? 'done' : 'not done' %>
          
          <form method="POST" action="/skills/<%= t.id %>?_method=DELETE">
            <button type="submit">X</button>
          </form>
        </li>
      <% }); %>
    </ul>
  </body>

</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Express All Skills</title>
</head>
<body>
    <h1>All Skills</h1>
    <ul>
      <% slills.forEach(function(t) { %>
        <li>
          <%= t.skill %>
            - 
          <%= t.done ? 'done' : 'not done' %>
        </li>
      <% }); %>
    </ul>
  </body>

</html>